Now I have the full picture. The authoritative source `#03 程式設計規格報告書 §4.2.2` lists **8 個階段**，合計 ≤3,300ms，餘裕 1,700ms。ch4 漏列了 API Gateway (≤100ms)、意圖辨識 (≤200ms)、回覆發送 (≤100ms) 三個階段，導致合計少 400ms、餘裕多 400ms。

文件中有**兩處**受影響（同一根因）：

---

### 修正 1：§4.15 延遲預算合計與餘裕與 #03 §4.2.2 不一致（漏列 3 個階段）
位置：約第 409 行 / §4.15 模組間介面總覽
原文：<<<
AI-Agent 文字對話延遲預算：個資遮罩 ≤50ms → 向量檢索 ≤200ms → Prompt 組裝 ≤50ms → LLM 推論 ≤2,500ms → Citation 抽取 ≤100ms → 合計 ≤2,900ms，餘裕 2,100ms 內完成 5,000ms SLA。
>>>
改為：<<<
AI-Agent 文字對話延遲預算：API Gateway ≤100ms → 意圖辨識 ≤200ms → 個資遮罩 ≤50ms → 向量檢索 ≤200ms → Prompt 組裝 ≤50ms → LLM 推論 ≤2,500ms → Citation 抽取 ≤100ms → 回覆發送 ≤100ms → 合計 ≤3,300ms，餘裕 1,700ms 內完成 5,000ms SLA。
>>>

### 修正 2：§4.3 計算條件延遲預算與 #03 §4.2.2 不一致（同根因）
位置：§4.3 P-AGT-03 處理邏輯表格「計算條件」欄
原文：<<<
端到端回應 ≤5,000ms（個資遮罩 ≤50ms + 向量檢索 ≤200ms + Prompt 組裝 ≤50ms + LLM 推論 ≤2,500ms + Citation 抽取 ≤100ms + 餘裕 2,100ms）
>>>
改為：<<<
端到端回應 ≤5,000ms（API Gateway ≤100ms + 意圖辨識 ≤200ms + 個資遮罩 ≤50ms + 向量檢索 ≤200ms + Prompt 組裝 ≤50ms + LLM 推論 ≤2,500ms + Citation 抽取 ≤100ms + 回覆發送 ≤100ms = 3,300ms，餘裕 1,700ms）
>>>

---

**說明：** 修正依據為 `#03 程式設計規格報告書 §4.2.2` 完整 8 階段延遲預算表（合計 ≤3,300ms，餘裕 1,700ms）。問題清單僅列修正 1，但修正 2 為同一根因的連帶不一致，建議一併修正。
