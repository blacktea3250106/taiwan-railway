# 系統上線執行計畫書

**案號：** L0215P2010U
**版本：** 1.0
**日期：** 中華民國 115 年

---

已完成 `08_系統上線執行計畫書_ch1-2.md`，輸出至 `pipeline/shared_state/assembled/`。

**§8.1 上線策略與總體規劃** 涵蓋：
- 灰度上線策略（10% → 30% → 100%）與核心考量
- D+181 ~ D+270 完整時程表（含灰度三階段、教育訓練、驗收）
- 時程圖（ASCII）
- Go/No-Go 判定標準五項指標（正確率/回應時間/可用性/MTTR/轉人工比例）
- 回滾方案（SIP 路由切換 ≤5 分鐘，AI 退場不影響人工客服）

**§8.2 上線範圍與工作項目** 涵蓋：
- 六大子系統上線範圍總覽（AI-IVR / AI-Agent / AI-QI / SMS / 報表 / WS+ADM），標註 FR 編號與灰度階段
- 各子系統上線內容明細（含技術選型與 KPI 目標）
- 19 項上線工作項目清單（五大類 × 時序展開）
- 工作相依關係圖
- 10 套既有系統介接上線順序（風險由低至高）

自然防禦了 ATK-005（E1 介接）、ATK-008（AI 錯誤三層防線）、ATK-009（避開尖峰）、ATK-010（轉人工監控）、ATK-019（API 規格 Adapter Pattern）、ATK-020（知識庫就緒）等攻擊面。全文約 3,200 字。


---

已完成 `08_系統上線執行計畫書_ch3-4.md`，輸出至 `pipeline/shared_state/assembled/`。

**§8.3 實施方法** 涵蓋：
- 灰度上線實施架構：SIP 路由層與 CRM Gateway 訊息路由層雙軌流量控制機制
- 全量切換七步驟（含切換前確認清單、逐項啟用間隔 10 分鐘、即時監控表）
- 回滾計畫：局部回滾（單一子系統退場，≤5 分鐘）與全面回滾（全 AI 退場），皆不影響既有人工客服
- 三級降級策略（L1 減少實例 / L2 HA 切換 / L3 API 不可用降級）

**§8.4 軟體版本說明與上線後觀察期** 涵蓋：
- 完整軟體版本清單（AI 模型/資料庫/應用框架/監控部署，全數引用 decisions.yaml）
- 版本管理策略：Docker 映像檔語意化標籤、版本凍結原則（上線前 14 天）、升級/回滾操作流程
- 上線後觀察期三階段（密集 15 天 → 穩定 20 天 → 驗收過渡 20 天），含具體監控頻率、駐點安排、調校重點
- 觀察期異常 P1~P4 分級處理流程

自然防禦了 ATK-008（正確率即時監控）、ATK-009（非尖峰切換）、ATK-010（轉人工比例）、ATK-011（Coqui 備案）、ATK-014（HA 切換）、ATK-019（API 降級）等攻擊面。全文約 3,500 字。


---

