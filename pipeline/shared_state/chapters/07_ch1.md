Now I have all the data needed. Let me write the chapter.

---

# 第一章　系統架構摘述

## 1.1　架構設計總覽

本系統採用六層式架構設計（AD-001），將 AI 客服功能與台鐵既有系統完全隔離，確保導入過程對現行客服作業零衝擊。六層由下至上依序為：

| 層級 | 名稱 | 主要職責 |
|:---:|------|---------|
| L1 | 基礎設施層 | 伺服器硬體、Docker 容器運行環境、網路與儲存 |
| L2 | 平台安全層 | AD/LDAP 認證、RBAC 權限控管、日誌稽核、加密傳輸 |
| L3 | 資料管理層 | PostgreSQL 關聯資料庫、Qdrant 向量資料庫、Redis 快取 |
| L4 | AI 服務層 | LLM 推論（vLLM）、STT 語音辨識（faster-whisper）、TTS 語音合成（Coqui XTTS-v2）、RAG 檢索 |
| L5 | 通道接入層 | SIP/RTP 語音接入、CRM Gateway 文字接入、SMS 簡訊接入 |
| L6 | 使用者層 | 值機平台（WS）、管理後台（ADM）、旅客端對話介面 |

各層之間透過明確定義之 API 介面溝通：外部介接採用 REST API（符合國發會共通性應用程式介面規範），內部高頻服務間通訊採用 gRPC 以降低延遲。

## 1.2　硬體部署配置

正式環境部署於台鐵機房，所有資料地端儲存，不進行跨境傳輸。主要設備配置如下：

| 設備角色 | 數量 | 關鍵規格 | 部署服務 |
|---------|:---:|---------|---------|
| AI-IVR 伺服器 | 2 台 | 16 核心 ≥2.2GHz ×2、DDR5 64GB+、960GB SSD ×3（RAID-5） | STT、TTS、SIP/RTP 語音處理（CPU 推論） |
| AI 伺服器 | 2 台 | 16 核心 ≥3.0GHz ×2、DDR5 128GB+、NVIDIA L40S 48GB ×2 | LLM 推論、Embedding 運算、質檢分析 |
| 管理/資安監控伺服器 | 2 台 | 16 核心 ≥3.0GHz ×2、DDR5 64GB+ | PostgreSQL、Qdrant、Redis、Prometheus、Grafana |
| SMS 簡訊收發裝置 | 2 台 | 256GB+儲存、4G LTE+ | 雙備援簡訊收發 |

GPU 資源分配策略（AD-002）：每台 AI 伺服器配備 2 張 L40S，GPU#0 專責 LLM 推論（vLLM + Llama 3.1 8B-Instruct AWQ，量化後 VRAM 用量約 8GB，單卡可運行 6 個推論實例），GPU#1 負責 Embedding 運算、質檢分析及擴充預留。STT 與 TTS 均採 CPU 推論（AD-003），部署於 AI-IVR 伺服器，30 路串流推論約需 9 CPU 核心，不佔用 GPU 資源。

## 1.3　高可用性架構

正式環境採 Active-Active + Failover 架構（AD-005）：

- **正常運行：** 兩台同類伺服器各承擔 50% 負載，透過負載均衡器分配請求。
- **故障切換：** 任一台故障時，存活機自動承載 100% 流量，以 Keepalived VRRP 實現虛擬 IP 漂移，偵測間隔 ≤2 秒、切換完成 ≤5 秒。
- **人工降級：** 當系統發生異常無法自動恢復時，系統立即切換為人工客服模式，所有來電直接路由至值機員，確保服務不中斷。

所有軟體服務以 Docker Compose 編排（AD-006），每台伺服器各自運行獨立的 `docker-compose.yaml`，維運人員可透過標準 Docker 指令操作服務啟停與狀態查看，詳細操作步驟請參閱第 7.8 章。

## 1.4　既有系統介接總覽

本系統以「零衝擊」原則介接台鐵既有環境，不修改既有系統之任何設定（AD-007）：

```
┌──────────────────────────────────────────────────────────────┐
│                        既有系統                              │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │ PSTN E1 │  │ WebCall  │  │   MES    │  │ CRM Gateway  │  │
│  │ Gateway │  │ SBC ×2   │  │ Server   │  │ (數位客服)    │  │
│  └────┬────┘  └────┬─────┘  └────┬─────┘  └──────┬───────┘  │
│       │SIP Trunk   │SIP Trunk    │RTP fork       │REST/WS   │
└───────┼────────────┼─────────────┼───────────────┼──────────┘
        ▼            ▼             ▼               ▼
┌──────────────────────────────────────────────────────────────┐
│                    AI 客服系統（本案建置）                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐ │
│  │ AI-IVR   │  │ AI-Agent │  │  AI-QI   │  │  管理後台     │ │
│  │ 語音應答  │  │ 文字客服  │  │ 智慧質檢  │  │  (ADM)       │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

各介接通道之技術細節（SIP 信令流程、RTP fork 機制、WebCall SBC 雙備援 failover 等），請參閱程式設計規格報告書（#03）第四章。

---

# 第二章　系統流程圖與使用者權限

## 2.1　AI-IVR 語音互動流程

旅客透過 PSTN、WebCall 或行政分機撥入後，AI-IVR 之處理流程如下：

```
旅客撥入
    │
    ▼
┌────────────────┐
│ SIP/RTP 接收    │ ← MES Server RTP fork（被動接收，不攔截原始通話）
│ G.711 解碼      │    延遲 ≤10ms
└───────┬────────┘
        ▼
┌────────────────┐
│ VAD 語音偵測    │ ← 偵測旅客發言結束時機
│ (M-STT-03)     │    延遲 ≤200ms
└───────┬────────┘
        ▼
┌────────────────┐
│ STT 語音辨識    │ ← faster-whisper 串流辨識（CPU 推論）
│ (M-STT-01)     │    延遲 ≤800ms
└───────┬────────┘
        ▼
┌────────────────┐
│ 意圖辨識        │ ← 文字意圖分類與路由決策
│ (M-STT-02)     │    延遲 ≤200ms
└───────┬────────┘
        ▼
┌────────────────┐
│ 個資遮罩        │ ← 身分證/電話/信用卡號正則遮罩
│                │    延遲 ≤50ms
└───────┬────────┘
        ▼
┌────────────────┐     信心度 ≥0.85 → 直接回答
│ LLM 推論       │ ←  信心度 0.6~0.85 → 回答＋提示洽人工
│ (M-LLM-01~02) │     信心度 <0.6 → 轉接真人客服
│                │    延遲 ≤2,500ms
└───────┬────────┘
        ▼
┌────────────────┐
│ TTS 語音合成    │ ← Coqui XTTS-v2 串流合成（CPU 推論）
│ (M-TTS-01)     │    首包延遲 ≤300ms，總延遲 ≤800ms
└───────┬────────┘
        ▼
┌────────────────┐
│ RTP 發送播放    │ ← 編碼封包後播放回覆語音
│                │    延遲 ≤50ms
└────────────────┘
    合計延遲 ≤4,600ms（SLA 上限 5,000ms，餘裕 400ms）
```

多輪對話透過 Redis 維護當次通話之 Session Context；通話結束時觸發滿意度調查，完整對話紀錄寫入 PostgreSQL。AI 無法解決之問題自動轉接真人客服，並附帶對話摘要與知識輔助資訊供值機員參考。

## 2.2　AI-Agent 文字對話流程

旅客透過 Web 或 App 發起文字對話後，AI-Agent 之處理流程如下：

```
旅客送出訊息
    │
    ▼
┌────────────────┐
│ API Gateway     │ ← 訊息接收、Session 識別
│                │    延遲 ≤100ms
└───────┬────────┘
        ▼
┌────────────────┐
│ 意圖辨識        │ ← 文字意圖分類、路由決策
│                │    延遲 ≤200ms
└───────┬────────┘
        ▼
┌────────────────┐
│ 個資遮罩        │ ← 正則遮罩去識別化
│                │    延遲 ≤50ms
└───────┬────────┘
        ▼
┌────────────────┐
│ RAG 語意檢索    │ ← Qdrant 向量資料庫 Top-K 檢索
│ (M-AGT-02~03) │    延遲 ≤200ms
└───────┬────────┘
        ▼
┌────────────────┐
│ Prompt 組裝     │ ← System Instruction + RAG 結果 + 歷史摘要
│                │    延遲 ≤50ms
└───────┬────────┘
        ▼
┌────────────────┐
│ LLM 推論       │ ← vLLM + Llama 3.1 8B AWQ
│ (M-LLM-01~02) │    延遲 ≤2,500ms
└───────┬────────┘
        ▼
┌────────────────┐
│ Citation 標註   │ ← 標註引用之知識來源出處
│ + 回應輸出      │
└────────────────┘
    合計延遲 ≤3,100ms（SLA 上限 5,000ms，餘裕 1,900ms）
```

AI 回應自動標註知識來源出處（RAG Citation），前端以 footnote 方式呈現。SMS 簡訊通道之 AI 自動回覆會同步透過 WebSocket 推播通知客服人員。

## 2.3　AI-QI 質檢批次流程

```
錄音檔匯入（手動/排程）
    │
    ▼
┌────────────────┐
│ 格式轉換與切割   │ ← 支援多種音檔格式
└───────┬────────┘
        ▼
┌────────────────┐
│ STT 轉寫       │ ← 業務場景術語優化
│ (M-QI-02)      │
└───────┬────────┘
        ▼
┌────────────────┐
│ 規則評分        │ ← 依自訂質檢規則自動評分
│ (M-QI-03)      │    含靜音/插話偵測、情緒分析
└───────┬────────┘
        ▼
┌────────────────┐
│ 質檢報告產出     │ ← 15 分鐘錄音 → ≤5 分鐘完成
│ (M-QI-07)      │    異常自動發送警報通知督導
└────────────────┘
```

## 2.4　使用者角色與存取權限

系統採角色型存取控制（RBAC），整合台鐵既有 Active Directory（AD/LDAP）進行帳號驗證（FR-ADM-002 / M-ADM-02），並透過企業資訊入口平台實現 SSO 單一登入。各角色之功能存取範圍如下：

| 功能模組 | 系統管理員 | 督導 | 值機員 | 外點人員 |
|---------|:--------:|:---:|:-----:|:------:|
| **值機平台** | | | | |
| 　來電接聽/通話控制 | — | ● | ● | — |
| 　AI 知識輔助面板 | — | ● | ● | — |
| 　對話摘要與工單管理 | — | ● | ● | — |
| 　數位客服文字對話 | — | ● | ● | — |
| 　SMS 簡訊檢視處理 | — | ● | ● | — |
| 　座席簽入/簽出/狀態切換 | — | ● | ● | — |
| **督導監控** | | | | |
| 　即時座席狀態監控 | — | ● | — | — |
| 　監聽/密語/強制介入 | — | ● | — | — |
| 　質檢規則設定與報表 | — | ● | — | — |
| 　即時服務指標查看 | — | ● | — | — |
| **知識庫管理** | | | | |
| 　知識庫內容維護（CRUD） | ● | ● | 唯讀 | — |
| 　Embedding 重建觸發 | ● | — | — | — |
| 　版本管理與回溯 | ● | ● | — | — |
| **報表功能** | | | | |
| 　統計分析報表 | ● | ● | 個人 | — |
| 　彈性儀表板 | ● | ● | — | — |
| 　報表匯出（Word/PDF/Excel/HTML/CSV） | ● | ● | 個人 | — |
| **系統管理** | | | | |
| 　系統參數設定 | ● | — | — | — |
| 　帳號與角色權限管理 | ● | — | — | — |
| 　Prometheus/Grafana 監控 | ● | — | — | — |
| 　日誌查詢與稽核追蹤 | ● | — | — | — |
| 　備份排程與回復操作 | ● | — | — | — |
| **後送案件** | | | | |
| 　案件查看與處理 | — | ● | — | ● |
| 　案件指派與追蹤 | — | ● | — | — |

> **●** 可存取　**唯讀** 僅可查看　**個人** 僅可查看自身相關紀錄　**—** 不可存取

所有角色之操作行為均寫入系統日誌，個資相關存取另行記錄於個資存取日誌，日誌保留期限 ≥6 個月。各角色之具體操作方式，請參閱第 7.3 章（值機平台）、第 7.4 章（督導監控）、第 7.6 章（系統管理）。

---

### 本章引用編號清單

| 類別 | 編號 |
|-----|------|
| 架構決策（AD） | AD-001（六層式架構）、AD-002（GPU 分配策略）、AD-003（STT/TTS 跑 CPU）、AD-004（Llama 3.1 8B AWQ）、AD-005（HA Active-Active + Failover）、AD-006（Docker Compose 部署）、AD-007（SIP/RTP fork 介接） |
| 功能需求（FR） | FR-IVR-001~007、FR-STT-001~003、FR-TTS-001~002、FR-LLM-001~008、FR-AGT-001~008、FR-QI-001~007、FR-WS-001~005、FR-ADM-001~005、FR-RPT-001~003 |
| 軟體模組（M） | M-IVR-01~06、M-STT-01~03、M-TTS-01~02、M-LLM-01~05、M-KB-01~02、M-AGT-01~06、M-SMS-01、M-QI-01~07、M-WS-01~05、M-ADM-01~05、M-RPT-01~03 |
| 攻擊面（ATK） | ATK-011（台鐵接手維運門檻）、ATK-024（文件格式合規） |
