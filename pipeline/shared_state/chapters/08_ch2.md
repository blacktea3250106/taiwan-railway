已完成 `08_系統上線執行計畫書_ch3-4.md`，輸出至 `pipeline/shared_state/assembled/`。

**§8.3 實施方法** 涵蓋：
- 灰度上線實施架構：SIP 路由層與 CRM Gateway 訊息路由層雙軌流量控制機制
- 全量切換七步驟（含切換前確認清單、逐項啟用間隔 10 分鐘、即時監控表）
- 回滾計畫：局部回滾（單一子系統退場，≤5 分鐘）與全面回滾（全 AI 退場），皆不影響既有人工客服
- 三級降級策略（L1 減少實例 / L2 HA 切換 / L3 API 不可用降級）

**§8.4 軟體版本說明與上線後觀察期** 涵蓋：
- 完整軟體版本清單（AI 模型/資料庫/應用框架/監控部署，全數引用 decisions.yaml）
- 版本管理策略：Docker 映像檔語意化標籤、版本凍結原則（上線前 14 天）、升級/回滾操作流程
- 上線後觀察期三階段（密集 15 天 → 穩定 20 天 → 驗收過渡 20 天），含具體監控頻率、駐點安排、調校重點
- 觀察期異常 P1~P4 分級處理流程

自然防禦了 ATK-008（正確率即時監控）、ATK-009（非尖峰切換）、ATK-010（轉人工比例）、ATK-011（Coqui 備案）、ATK-014（HA 切換）、ATK-019（API 降級）等攻擊面。全文約 3,500 字。
